# V3 S1-S5 完成度 + 一期 ↔ 二期分界

> **生成日期**：2026-05-29
> **范围**：基于 2026-05-29 commit `7d72eca0` 之前的 6 个 V3 增强 commit 汇总
> **来源**：直接读 Server/iOS/admin 代码 + Prisma schema + migration 文件验证

---

## ✅ 一期已落地（按 Sprint 分）

### S1 解锁基建（commit `47ba169f`）
1. **broadcast UNIQUE 部分索引**（migration 20260529000100）：
   `family_broadcasts_group_hash_day_unique(group_id, content_hash, date(created_at))`
   并发"同秒两人触发同号码"时由 DB 兜底拦截。
2. **AI 幂等 + Redis SETNX 锁**（family.service.createBroadcast）：
   抢锁 → 预查重 → 预查配额 → 调 AI → 入库（P2002 兜底）→ 释放。
   AI 调用始终在配额检查之后，最坏花 1 次钱。
3. **家庭套餐商品**：
   - iOS Local.storekit + ProductIdentifiers.swift 加 `family.monthly $14.99` / `family.annual $129.99`
   - MembershipPlan 加 `tier` 字段（migration 20260529000200）
   - seed.ts 录入 family-monthly / family-annual
4. **查询配额体系 + 家庭权益分发**（src/modules/quota/）：
   - EntitlementService：free / personal_pro / family_owner / family_member
   - QuotaService：Redis 计数，free 5/天，Pro / family 不限
   - QueryController 全部接入；FamilyService.isUserPro 改走 EntitlementService
5. **APNs 真实实现**：
   - HTTP/2 + ES256 JWT（Node 内建 crypto，dsaEncoding=ieee-p1363）
   - UserDevice 表 + POST /api/user/v3/devices（migration 20260529000300）
   - iOS PushService + PushAppDelegate（UIApplicationDelegateAdaptor）
   - 失败处理：410 Unregistered/BadDeviceToken 自动清理 token

### S2 联动收口（commit `8e9fdf76`）
1. **heartbeat trigger_source**（migration 20260529000400）：user_activities 加 JSONB 列；
   iOS 上报 `cold_launch` / `foreground`；push_tap 不传以符合 PRD"真实主动"语义。
2. **activeCount 日内仅记 1 次**：日内首次写 1，后续仅刷 lastActiveAt + 合并 trigger_sources。
3. **Query 高风险自动家庭广播**（query.service.maybeAutoBroadcast）：
   risk_level='high' fire-and-forget 调 createBroadcast(source='auto_query')；
   尊重 member.shareQueryResults 隐私开关（skipReason='disabled_by_user'）。
4. **A1 一键广播家庭**（deepfake.service.broadcastToFamily）：
   - POST /api/v3/deepfake/voice/:taskId/broadcast
   - content_hash 用 `voice:{taskId}` 保证当日唯一
   - score 映射 scam / safe / unknown
   - iOS DeepfakeResultView 占位接通（带 loading + 友好错误）
5. **SSE 实时结果推送**（替代 PRD WS /ws/deepfake，零新增依赖）：
   - NestJS @Sse + rxjs Observable，1.5s 轮询 DB，status 变化即吐帧
   - iOS DeepfakeStreamClient（URLSession.bytes）+ DeepfakeRepository.streamResult

### S3 合规闸门（commit `356cf168`）
1. **F 模块 region gate**（OverseaOnlyGuard）：CN 用户 403 + 60s 缓存。
2. **.env.example 补全**：APNS_* / ALIYUN_SMS_* / TWILIO_* / TURNSTILE / CF_ACCESS。
3. **COPPA 字段**（migration 20260529000500）：user 加 is_minor / parent_consent_at；
   redeemInviteCode 接受 parentConsent；iOS RedeemInviteSheet 加勾选框。
4. **onDelete 策略**（migration 20260529000600）：
   - family_groups.owner_user_id → CASCADE（owner 删 = 家庭组解散）
   - family_broadcasts.triggered_by_user_id 改 nullable + SET NULL（去匿名化）
5. **数据导出**（GET /api/auth/export-data）+ deleteAccount 注释级联范围。
6. **关怀按 user.regionCode 映射时区**（src/common/utils/region-timezone.ts）：
   cron 改 hourly，9-22 点窗口内推送。
7. **AdminAuditLog 真落表**（migration 20260529000700）：补 target_type / before / after /
   ip / userAgent 字段；AdminAuditLogService 统一入口；admin-users.disable/enable 已迁移。

### S4 体验补全（commit `26c28e8b`）
1. **F 模块 4 个细节页**：
   - BreachTargetDetailView（F-P3 监控目标详情 + 历史 + 删除）
   - BreachUpgradeSheet（F-P5 引导付费）
   - 复用 BreachAddTargetSheet（F-P2）+ BreachAlertDetailView（F-P4）
2. **admin IntelEdit 富文本 + AI 改写**：
   - Server `POST /admin/intel/ai-rewrite` 调 AiProviderService 输出结构化 contentBlocks
   - admin BlocksEditor 子组件（块类型 / 上下移动 / 删除 / 字数）
3. **E-P5 MemberDetailView**：
   - 头像 + 角色 + 活跃 + 远程长辈模式开关 + 拨号 + 隐私说明
   - APIEndpoint .v3FamilyMemberElderMode + FamilyRepository.setMemberElderMode
4. **关怀 push 失败检测 + 提前降级 SMS**（migration 20260529000800）：
   FamilyCareNotice 加 delivery_status JSONB；第 2 天 push 全失败 → 立刻 escalatedToSms。

### S5 多国 + 兜底（commit `7d72eca0`）
1. **content_hash 归一化**（src/common/utils/content-normalize.ts）：
   phone E.164（libphonenumber-js）/ URL canonical / NFKC 全半角统一。
2. **Twilio 按国家路由**：TWILIO_FROM_NUMBERS JSON 映射；印度 DLT / 巴西 ANATEL 等可单独配本地号。
3. **APNs collapse-id**：sendPush 入参 collapseId；
   broadcast 用 `broadcast:{id}`（二期撤回可替换）；
   care 用 `care:{userId}`（连续多天提醒锁屏只显最新）。
4. **iOS 数据导出 UI**：SettingsView 加按钮 → 拉 JSON → 写临时文件 → 弹 ShareSheet。
5. **文档签名【StarLens】统一**：V3一期上线配置清单.md 改 ALIYUN_SMS_SIGN_NAME。

---

## ⏸ 一期不带（已与代码事实对齐，推二期）

| 项 | 一期不带原因 | 二期需要 |
|---|---|---|
| 官方广播撤回 / 紧急下架 | FamilyBroadcast 无 is_recalled / recalled_at / recalled_by_admin_id 字段 | 加字段 + Admin 撤回接口 + iOS 端 status 过滤 + APNs collapseId 替换通知 |
| AI 模型版本追溯 | family_broadcast.result_detail 仅 confidence/features/advice | result_detail 补 modelVersion / provider / promptVersion，便于误报 RCA |
| 阿里云短信 SDK 真实集成 | HTTP API 签名复杂；代码内 stub | npm i @alicloud/dysmsapi20170525 + 重写 sendSmsAliyun |
| iOS Family Sharing 集成 | familyShareable 全 false；与自建家庭组冲突未定 | 决定 family.* 是否走 Apple 共享并处理 originalTransactionId 关联 |

---

## ⚠ V3 一期但部分实施 / 测试待补

- **APNs 实际端到端测试**：需在真机 + sandbox 环境验证 deviceToken 流转，至少 1 次成功推送
- **阿里云短信 / Twilio**：账号开通 + 签名 / 模板备案后才能真实发；当前 env 未配走 stub log
- **AI 改写 prompt 质量**：S4-2 用 stub 化提示词；上线前请编辑团队 review
- **F 模块 HIBP API**：BreachService 内未真实接 HIBP；扫描 cron 跑空（仍占 03:00 UTC schedule）
- **R2 24h 清理**：DeepfakeService 已写 cleanup cron，需配 R2 凭证才会真删

---

## 📊 commit 索引

| Sprint | commit | 文件 / 改动 |
|---|---|---|
| 接手前 V3 已实现 | `630bb1be` 起 | E/J/B/A1/F 五模块基础架构（详见 git log） |
| S1 | `47ba169f` | 25 文件 +1605/-358 |
| S2 | `8e9fdf76` | 15 文件 +578/-64 |
| S3 | `356cf168` | 23 文件 +687/-65 |
| S4 | `26c28e8b` | 17 文件 +1051/-16 |
| S5 | `7d72eca0` | 8 文件 +310/-15 |
| 文档同步 | （本 commit） | docs/V3 三件套对齐到代码现状 |

总计 6 个 V3 增强 commit，约 +4231 / -518。
